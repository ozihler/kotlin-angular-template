language: java
jdk:
  - openjdk12
before_install:
  - chmod +x gradlew

jobs:
  include:
    - stage: test
      script:
    - ./gradlew check
    - stage: build
      script:
    - ./gradlew build --scan -s
#deploy:
#  provider: heroku
#  api-key:
#    secure: $HEROKU_API_KEY